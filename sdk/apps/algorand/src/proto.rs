// Include generated message-id and button-request enums (see `build.rs`);
// generated from `protob/messages.proto` and `protob/common.proto` so the
// Rust codes cannot drift from the wire spec.
include!(concat!(env!("OUT_DIR"), "/proto_ids.rs"));
